<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>imgUpload</title>
    <script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.8.0.js"></script>
    <style media="screen">
        .content {
            width: 50%;
            margin: 0 auto;
        }

        .title {
            color: rgb(7, 17, 27);
        }

        .fromConten {
            margin-top: 40px;
        }

        .imgFile {
            width: 60%;
            left: 0;
            top: 0;
        }

        .label {
            display: block;
            font-size: 24px;
            font-weight: 700;
            line-height: 24px;
            padding: 20px 0;
        }

        .imgInput {
            position: absolute;
            display: block;
            opacity: 0;
            width: 200px;
            height: 20px;
            padding: 4px 10px;
            left: 0;
            top: 0;
        }

        .upload {
            position: relative;
            display: inline-block;
            vertical-align: top;
            padding: 4px 10px;
            width: 200px;
            height: 20px;
            line-height: 20px;
            border: 1px solid #999;
            text-decoration: none;
            color: #666;
        }

        .buttom {
            display: inline-block;
            width: 222px;
            height: 30px;
            line-height: 20px;
            border: 1px solid #999;
        }
    </style>
</head>

<body>
    <div class="content">
        <div class="title">
            <h1>人员信息</h1>
            <%if(success===1){%>
                <h2>上传成功！</h2>
                <%}%>
            <%if(success===2){%>
                <h2>选择文件不能为空！</h2>
                <%}%>
        </div>
        <div class="fromConten">
            <form method="post" enctype="multipart/form-data">
                <div class="info">  
                </div>
                <div class="imgFile">
                    <label class="label">姓名:</label>
                    <input type='text' name="name" id="name">
                    <label class="label">警号:</label>
                    <input type='text' name="id" id="id">
                    <label class="label">性别:</label>
                    <input type='text' name="gender" id="gender">
                    <label class="label">部门:</label>
                    <input type='text' name="department" id="department">
                    <label class="label">选择图片:</label>
                    <span class="upload"><span class="show">单击选择文件</span>
                    <input class="imgInput" type="file" name="img">
                    </span>
                    <input type="submit" class="buttom" value="上传">
                </div>
            </form>
        </div>
    </div>
    <a href='/year'>返回</a>
</body>
</html>
<script>
    $(document).ready(function() {
        $(".imgInput").change(function() {
            var arrs = $(this).val().split('\\');
            var filename = arrs[arrs.length - 1];
            $(".show").text(filename);
        });
        $(".buttom").on('click',function() {
            $.ajax({
                url:'/new',
                type:'post',
                data:{
                    name:$('#name').val(),
                    id:$('#id').val(),
                    gender:$('#gender').val(),
                    department:$('#department').val()
                },
                success:function(res){
                    console.log(res);
                }
            });
        });
    });
</script>